import 'package:flutter/material.dart';

class FMTabBarVC extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('TabBar'),
      ),
      body: Container(),
    );
  }
}