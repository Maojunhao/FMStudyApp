import 'package:flutter/material.dart';

class FMTabbarViewVC extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('TabBarView'),
      ),
      body: Container(),
    );
  }
}