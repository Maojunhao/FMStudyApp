import 'package:flutter/material.dart';

class FMLinearProgressIndicatorVC extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text("LinearProgressIndicator"),),
    );
  }
}