import 'package:flutter/material.dart';

class FMDataTableVC extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text("DataTable"),),
    );
  }
}